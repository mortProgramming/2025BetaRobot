package frc.robot.commands.actions.CoralCorral.Gate;
import frc.robot.config.constants.PhysicalConstants.CoralCorralConstants;
import frc.robot.subsystems.CoralCorral;
import frc.robot.commands.actions.CoralCorral.Gate.setCoralCorral;
public class ascendCoralCorral {
    private CoralCorral coralCorral;
    public static void AscendCoralCorral(){
        //coralCorral=CoralCorral.getInstance();
    }
    public static void initialize(){
       /*  if (CoralCorral.getPositionLevel()==1){
            setCoralCorral.Level_2();
            CoralCorral.setPositionLevel(2);
        }
        if (CoralCorral.getPositionLevel()==2){
            setCoralCorral.Level_3();
            CoralCorral.setPositionLevel(2);
        }
        if (CoralCorral.getPositionLevel()==1){
            setCoralCorral.Level_2();
            CoralCorral.setPositionLevel(2);
        }
        if (CoralCorral.getPositionLevel()==1){
            setCoralCorral.Level_2();
            CoralCorral.setPositionLevel(2);
        }
        if (CoralCorral.getPositionLevel()==1){
            setCoralCorral.Level_2();
            CoralCorral.setPositionLevel(2);
        }*/
    }
}
